{
  "name": "ethoscan",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && bun run build:content && bun run build:popup && bun run copy:files",
    "build:content": "vite build --config vite.content.config.js",
    "build:popup": "vite build --config vite.popup.config.js",
    "copy:files": "cp manifest.json dist/manifest.json && cp dist/src/popup/popup.html dist/popup.html && cp public/icon.png dist/icon.png && mkdir -p dist/assets && cp src/assets/*.svg dist/assets/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "0.1.33",
    "daisyui": "5.5.14",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@tailwindcss/vite": "4.1.18",
    "tailwindcss": "4.1.18"
  }
}
